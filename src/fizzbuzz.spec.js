import fizzbuzz from "./fizzbuzz";


describe("Fizz Buzz", () => {
  it("Nada", () => {
    expect(fizzbuzz(1)).toEqual("1");
  });
  it("Nada", () => {
    expect(fizzbuzz(2)).toEqual("2");
  });
});